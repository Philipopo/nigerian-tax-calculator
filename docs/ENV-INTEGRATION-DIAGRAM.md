# Environment Validation Integration Diagram

## Visual Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         APP STARTUP                             │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  app/layout.tsx:10-12                                           │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ import '@/lib/env'                                        │  │
│  │ ↓                                                         │  │
│  │ This import triggers validation immediately              │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  src/lib/env.ts                                                 │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ const env = validateEnv()                                │  │
│  │ ├─ validateClientEnv()                                   │  │
│  │ │  ├─ Check NEXT_PUBLIC_SUPABASE_URL                     │  │
│  │ │  ├─ Check NEXT_PUBLIC_SUPABASE_ANON_KEY                │  │
│  │ │  ├─ Check NEXT_PUBLIC_PLASMIC_PROJECT_ID               │  │
│  │ │  ├─ Check NEXT_PUBLIC_PLASMIC_TOKEN                    │  │
│  │ │  ├─ Check NEXT_PUBLIC_RECAPTCHA_SITE_KEY               │  │
│  │ │  ├─ Check NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY              │  │
│  │ │  ├─ Check NEXT_PUBLIC_REMITA_MERCHANT_ID               │  │
│  │ │  ├─ Check NEXT_PUBLIC_REMITA_SERVICE_TYPE_ID           │  │
│  │ │  ├─ Check NEXT_PUBLIC_OPAY_MERCHANT_ID                 │  │
│  │ │  ├─ Check NEXT_PUBLIC_OPAY_PUBLIC_KEY                  │  │
│  │ │  ├─ Check NEXT_PUBLIC_APP_ENV                          │  │
│  │ │  └─ Check NEXT_PUBLIC_APP_URL                          │  │
│  │ │                                                         │  │
│  │ └─ validateServerEnv() (server-side only)                │  │
│  │    ├─ Check SUPABASE_SERVICE_ROLE_KEY                    │  │
│  │    ├─ Check RECAPTCHA_SECRET_KEY                         │  │
│  │    ├─ Check PAYSTACK_SECRET_KEY                          │  │
│  │    ├─ Check REMITA_API_KEY                               │  │
│  │    └─ Check OPAY_SECRET_KEY                              │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                    ┌─────────┴─────────┐
                    │                   │
                 ✅ PASS             ❌ FAIL
                    │                   │
                    ↓                   ↓
      ┌─────────────────────┐  ┌──────────────────────┐
      │ Export validated    │  │ Throw detailed error │
      │ env object          │  │ with instructions    │
      └─────────────────────┘  └──────────────────────┘
                    │                   │
                    ↓                   ↓
      ┌─────────────────────┐  ┌──────────────────────┐
      │ App continues       │  │ Error is thrown      │
      │ normally            │  │ during import        │
      └─────────────────────┘  └──────────────────────┘
                    │                   │
                    ↓                   ↓
      ┌─────────────────────┐  ┌──────────────────────┐
      │ layout.tsx renders  │  │ EnvErrorBoundary     │
      │                     │  │ catches error        │
      │ <EnvErrorBoundary>  │  │ (componentDidCatch)  │
      │   <App />           │  └──────────────────────┘
      │ </EnvErrorBoundary> │            │
      └─────────────────────┘            ↓
                    │          ┌──────────────────────┐
                    │          │ EnvErrorDisplay      │
                    │          │ renders beautiful    │
                    │          │ error UI with:       │
                    │          │ - Missing vars list  │
                    │          │ - Empty vars list    │
                    │          │ - Fix instructions   │
                    │          │ - Retry button       │
                    │          └──────────────────────┘
                    ↓                   │
      ┌─────────────────────┐           │
      │ User sees app       │  User sees error screen
      └─────────────────────┘           │
                                        ↓
                              Developer fixes .env.local
                                        ↓
                              Clicks "🔄 Retry" button
                                        ↓
                              Page reloads → Validation runs again
```

## Component Relationship

```
app/layout.tsx
├─ Imports '@/lib/env' (triggers validation)
│
└─ Renders:
   <html>
     <body>
       <EnvErrorBoundary>          ← Catches validation errors
         <PlasmicRootProvider>
           {children}
         </PlasmicRootProvider>
       </EnvErrorBoundary>
     </body>
   </html>
```

## Error Handling Flow

```
┌────────────────────────────────────────────────────────────┐
│ Validation Error Thrown                                    │
│ (from src/lib/env.ts)                                      │
└────────────────────────────────────────────────────────────┘
                           ↓
┌────────────────────────────────────────────────────────────┐
│ EnvErrorBoundary.componentDidCatch()                       │
│ ├─ Logs error to console                                  │
│ ├─ Updates state: { hasError: true, error }               │
│ └─ Triggers re-render                                      │
└────────────────────────────────────────────────────────────┘
                           ↓
┌────────────────────────────────────────────────────────────┐
│ EnvErrorBoundary.render()                                  │
│ └─ Returns: <EnvErrorDisplay error={this.state.error} />  │
└────────────────────────────────────────────────────────────┘
                           ↓
┌────────────────────────────────────────────────────────────┐
│ EnvErrorDisplay Component                                  │
│ ├─ Parses error.message                                   │
│ ├─ Extracts missing/empty variables                       │
│ └─ Renders styled error UI                                │
└────────────────────────────────────────────────────────────┘
                           ↓
┌────────────────────────────────────────────────────────────┐
│ Developer sees beautiful error screen in browser          │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ ⚙️ Environment Configuration Required                 │ │
│ │                                                        │ │
│ │ Missing variables:                                     │ │
│ │   - NEXT_PUBLIC_PLASMIC_PROJECT_ID                     │ │
│ │                                                        │ │
│ │ Empty variables:                                       │ │
│ │   - NEXT_PUBLIC_RECAPTCHA_SITE_KEY                     │ │
│ │   - NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY                    │ │
│ │                                                        │ │
│ │ 💡 Quick Fix:                                          │ │
│ │ 1. cp .env.example .env.local                          │ │
│ │ 2. Fill in missing values                              │ │
│ │ 3. npm run dev                                         │ │
│ │                                                        │ │
│ │                                       [🔄 Retry]       │ │
│ └────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
```

## File Locations

```
project-root/
│
├─ app/
│  ├─ layout.tsx                    ← Integration point (line 10 & 34)
│  └─ components/
│     ├─ EnvErrorBoundary.tsx       ← Catches errors
│     └─ EnvErrorDisplay.tsx        ← Renders error UI
│
├─ src/lib/
│  └─ env.ts                        ← Validation logic
│
├─ .env.local                       ← Your actual values (gitignored)
└─ .env.example                     ← Template (committed to git)
```

## Code Integration Points

### Point 1: Import Validation
**File:** `app/layout.tsx`
**Lines:** 9-12

```typescript
// Validate environment variables on app startup
// This will throw an error if any required variables are missing
// The EnvErrorBoundary will catch and display these errors gracefully
import '@/lib/env';
```

### Point 2: Error Boundary Wrapper
**File:** `app/layout.tsx`
**Lines:** 34-38

```typescript
<EnvErrorBoundary>
  <PlasmicRootProvider loader={PLASMIC}>
    {children}
  </PlasmicRootProvider>
</EnvErrorBoundary>
```

### Point 3: Validation Logic
**File:** `src/lib/env.ts`
**Lines:** Throughout file

```typescript
function validateClientEnv(): ClientEnv { ... }
function validateServerEnv(): ServerEnv { ... }
export const env = validateEnv();
```

## Usage in Application Code

```
┌─────────────────────────────────────────────────────────┐
│ Any Component/Page/API Route                            │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ import { env } from '@/lib/env'                     │ │
│ │                                                     │ │
│ │ const supabaseUrl = env.NEXT_PUBLIC_SUPABASE_URL   │ │
│ │                           ↑                         │ │
│ │                    TypeScript autocomplete!         │ │
│ │                    Type-safe access!                │ │
│ │                    Already validated!               │ │
│ └─────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

## Testing Flow

```
Developer runs: node scripts/test-env-validation.mjs
                              ↓
Script reads .env.local and checks each variable
                              ↓
                    ┌─────────┴─────────┐
                    │                   │
              Has Value            Empty/Missing
                    │                   │
                    ↓                   ↓
              Shows ✅              Shows ⚠️/❌
                    │                   │
                    └─────────┬─────────┘
                              ↓
              Displays demo error message
                              ↓
         Developer sees exactly what needs fixing
```

## Summary

**3 Key Integration Points:**

1. **Import Trigger** - `app/layout.tsx:10-12`
   - Imports validation module
   - Validation runs immediately
   - Throws error if variables missing

2. **Error Boundary** - `app/layout.tsx:34-38`
   - Wraps app content
   - Catches validation errors
   - Displays error UI

3. **Validation Logic** - `src/lib/env.ts`
   - Checks all required variables
   - Provides TypeScript types
   - Exports validated env object

**Developer Experience:**
- ✅ Immediate feedback on missing config
- ✅ Beautiful error UI with instructions
- ✅ Type-safe environment variable access
- ✅ No runtime surprises
